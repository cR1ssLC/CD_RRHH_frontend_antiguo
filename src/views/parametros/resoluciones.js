// material-ui
import { Typography } from '@mui/material';

// ==============================|| PAGE ||============================== //

const Resoluciones = () => (
    <Typography variant="h1" color="#212121" align="center" gutterBottom>
        RESOLUCIONES REGISTRADAS XD
    </Typography>
);

export default Resoluciones;
